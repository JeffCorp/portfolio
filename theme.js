import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        mode: 'dark',
        primary: {
            main: '#0a1a2f',
            dark: '#fff',
            grey: '#8893af',
        },
        secondary: {
            main: '#5F499E'
        }
    },
    typography: {
        
    }
})

export default theme;